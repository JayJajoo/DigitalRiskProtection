# ── Core web / runtime ──────────────────────────────────────────────
# fastapi 0.139+ / starlette 1.x required for compatibility with the mcp dep of claude-agent-sdk.
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
httpx==0.27.2
pillow==10.4.0              # Phase 1 — image seeding / handling
openai==1.51.0             # Phase 2 — embeddings
elasticsearch==8.15.0      # Phase 2 — keyword/fuzzy index
chromadb==0.5.5            # Phase 2 — vector store
claude-agent-sdk==0.2.122  # Phase 4/6 — enrichment + classification (needs the Claude Code CLI on PATH at runtime)
